{
  "name": "@what-to-eat/core",
  "version": "1.0.0",
  "description": "Shared TypeScript core module for business logic and API services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Linting core...'"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
